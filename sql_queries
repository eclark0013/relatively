SELECT 
    movie_id,
    user_id,
    rating
FROM ratings a JOIN 
    (
        SELECT 
            user_id,
            AVG(rating) as user_avg
        FROM ratings
            GROUP BY user_id
    ) b 
    ON a.user_id = b.user_id
    WHERE movie_id=1